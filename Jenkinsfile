pipeline {
  agent {
    lable "mineslave"
  }
    stages {
      stage('muni') {
        steps {
          sh "echo muni"
        }
      }
      stage('hema') {
        steps {
          sh "echo hema"
        }
      } 
    }
}
